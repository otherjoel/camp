#lang racket/base

(require punct/doc)

(provide feed-content)

;; Returns an X-expression of the HTML content of the item as it appears
;; in the RSS/Atom feed.
(define (feed-content doc ctxt)
  `(article ,@"@"(document-body doc)))
