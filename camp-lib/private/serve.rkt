#lang racket/base

(require racket/path)

(provide start-server)

(define (start-server output-folder
                       #:port [port 8000]
                       #:watch? [watch? #t])
  ;; TODO: Implement
  (void))
